[build.env]
# Allow release workflows to inject the tag version into builds running inside
# cross containers, so runtime-reported versions stay aligned with Git tags.
passthrough = ["XIMONITOR_BUILD_VERSION"]
