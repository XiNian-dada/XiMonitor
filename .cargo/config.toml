# 本地交叉编译时使用 rust-lld 作为 linker(避免依赖系统 musl 工具链)。
# CI 中会在 cross 构建前临时挪走本文件,以免与 cross 容器内的 linker 冲突。

[target.x86_64-unknown-linux-musl]
linker = "scripts/rust-lld-linker"

[target.aarch64-unknown-linux-musl]
linker = "scripts/rust-lld-linker"

